export const toastVariants = {
  destructive: {
    style: {
      color: 'var(--destructive)',
      borderColor: 'var(--border)',
      alignItems: 'flex-start',
      gap: '12px',
    },
  },
} as const
